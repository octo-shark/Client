/*
  This page should handle the login of existing users,
  As well as the account creation of new users.
  Once a user is properly logged in it should redirect to genericView.
*/

const Login = () => (
  <div>
    <p>
      This page handle auth api calls.
    </p>
  </div>
)

export default Login;